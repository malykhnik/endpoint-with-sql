spring:
  application:
    name: endpoint-with-sql
  datasource:
    url: jdbc:postgresql://sloth-2.suslovd.ru:9132/todo_list
    username: user
    password: pass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    database: postgresql